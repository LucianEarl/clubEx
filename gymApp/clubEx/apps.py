from django.apps import AppConfig


class ClubexConfig(AppConfig):
    name = 'clubEx'

"""
django.apps is a registry of installed applications that stores
configuration. 
AppConfig is a configuration within the registry
that is being imported.
ClubexConfig is a class that is being assigned with a variable.
"""